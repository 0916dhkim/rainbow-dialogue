// Read one line from stdin
// and wrap the string.
// Return s, or NULL if EOF.
// maxlinelength = limit where string gets wrapped (not counting newline)
// maxlinecount = number of lines allowed in one dialogue
char *get_next_dialogue(char *s, int maxlinelength, int maxlinecount);
