// Return string formatted inside a box.
// maxlinelength = limit where string gets wrapped (not counting newline)
// maxlinecount = number of lines allowed in one dialog
char *box_message(char* out, char *original, int maxlinelength, int maxlinecount);
